spring:
  application:
    name: class-booking-service
  datasource:
    url: jdbc:mysql://localhost:3306/booking_system?connectionTimeZone=UTC&forceConnectionTimeZone=true
    username: root
    password: Thisisme@123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    open-in-view: false

  jackson:
    time-zone: UTC
